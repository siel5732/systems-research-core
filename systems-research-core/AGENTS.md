# AGENTS.md — The Sovereign Agentic Governance & Epistemic Protocol (SAGE)
## Logos OS Sovereign Control Plane Specifications (v1.0.0)

> "Let there be no 'AI theater' here. We build under the absolute invariant of verifiable local execution, sovereign control of weights, and mathematical proof-gating of actions." 
> — Sefirotic Council Inscription

This document defines the formal protocol, architectural rules, and operational boundaries for all local agentic entities operating within **Logos OS**. It establishes a self-contained, offline-first **Proof Economy** and defines the unified evaluation criteria for the dual-core world models (**Mimir** and **Freya**).

---

## 1. The Sefirotic Council (Active Local Agent Registry)

All local agents must register with the system control plane and bind to local resource paths. They communicate via the unified MCP (Model Context Protocol) transport and route reasoning through local quantized inference (e.g., `PINQWEN-3.6-35B-CLEAN` via Ollama).

| Agent ID | Sovereign Role | Primary Cognitive Engine | Tool Domain Access | Security Level |
| :--- | :--- | :--- | :--- | :--- |
| **Metatron** | Control Plane Scribe & Master Orchestrator | `PINQWEN-3.6-35B` (IQ3_M) | System-wide, Gateway Config, Cron | LEVEL-0 (Root Sovereignty) |
| **Raziel** | Epistemic Grounding & Memory Sentinel | Local Embedding Engine / ChromaDB | MEMORY.md, preconscious_buffer.md | LEVEL-1 (Read/Write Memory) |
| **Binah** | Code Synthesis & Sandboxed Interpreter | Local Codegen Core | Sandbox Sandbox (`the-grid` PTY) | LEVEL-1 (Isolated Execution) |
| **Mimir-1** | Real-Time Latent Transition World Model | `latent_world_model_simulation.py` | Sensorimotor transitions ($t \to t+1$) | LEVEL-2 (Simulation Core) |
| **Freya-1** | Counterfactual Horizon World Model | `cogtest_world_models.py` | Temporal branching ($t \to t+H$) | LEVEL-2 (Simulation Core) |

---

## 2. Epistemic Memory Provenance Matrix

To prevent cognitive drift, hallucination accumulation, and memory corruption across deep multi-turn sessions, Logos OS enforces **Verifiable Memory Updates**.

### A. Memory Mutation Invariants
1. **The Double-Witness Invariant**: No agent may write directly to `MEMORY.md` without first staging the memory mutation in `preconscious_buffer.md`.
2. **Provenance Attestation**: Every memory block committed to `MEMORY.md` must append an **Epistemic Trace** containing:
   - **Originator**: The ID of the agent proposing the update.
   - **Witness**: The verifying peer agent (e.g., Raziel).
   - **Grounding Hash**: A hash of the source document or local verification trace that grounded the decision.
   - **Confidence Metric**: Real value $[0.0, 1.0]$.

### B. Verification Flow
```
[Inbound Information] ──> [Preconscious Buffer] ──> [Peer Peer Review (Grok/Local)] ──> [Fiat-Shamir Witness Verification] ──> [MEMORY.md Commit]
```

---

## 3. The Proof Economy & Action-Gated Sandboxes

Logos OS rejects post-hoc content filtering in favor of **Structural Security**. Agents do not have unrestricted execution privileges; instead, their capabilities are metered by a deterministic proof economy.

1. **Token Lease Protocol**: Running non-trivial operations (e.g., writing to system configurations, initiating large downloads, triggering physical hardware calls) requires a **Lease Ticket** signed by the gateway.
2. **Deterministic Sandboxing**: All tool invocations must run within POSIX-isolated namespaces on `the-grid`. 
   - No direct socket connections allowed for level-2 agent processes.
   - No access to `/etc` or root directory trees outside `/home/fq9f/systems-research-core`.
3. **Witness Rotations (Fiat-Shamir Zero-Knowledge Validation)**: Code generated by Binah must undergo a structural parsing pass to check for tracking scripts, telemetry, and unapproved API routes before being staged for GEEKOM deployment.

---

## 4. LOGOS-Bench: Unified World Model Metrics

We codify our local alternatives to paper-bound evaluations via a physical benchmark run locally on our node.

### A. Physics-IQ (Evaluated on Mimir)
Measures the mechanical and physical consistency of simulated latent spaces.
$$\text{Physics-IQ} = \max\left(0.0, 1.0 - \left(\bar{E}_{base} \times 0.15 + V_{drift} \times 0.20\right)\right)$$
Where:
- $\bar{E}_{base}$: Mean absolute transition error across state-vectors $[x, y, v_x, v_y]$.
- $V_{drift}$: Velocity drift penalty.

### B. WorldModelBench (Evaluated on Freya)
Measures alternative-timeline branching consistency and entropy leakage across deep temporal horizons $H$.
$$\text{Coherence Score} = \max\left(0.0, 1.0 - \sum_{i=1}^{H} \mathcal{H}_i\right)$$
Where:
- $\mathcal{H}_i$: Entropy leak factor calculated per branching timeline. High-entropy hand-wavy counterfactuals trigger a $0.35$ penalty, while mathematically anchored pathways (such as cartilage module decay with specific volumetric threshold metrics) are penalized only $0.05$.

---

## 5. Deployment & Open Source Intent

To support the broader sovereign homelab community, selected non-sensitive modules of Logos OS will be compiled and published on Hugging Face:
- **`siel5732/logos-world-model-bench`**: The local evaluation python harness.
- **`siel5732/logos-sage-mcp`**: The master Model Context Protocol configs and transport layer proxies.

---
*Document Authenticity Hash: 0x9f7fab311a641f99778523cb860be7*
